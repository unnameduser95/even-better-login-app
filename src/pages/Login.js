import React, { Component } from 'react';
import { Input, Text, StyleSheet, View } from 'react-native';

export default class Login extends Component {
  render() {
    return (
      <View>
        <Text>Hello there</Text>
      </View>
    )
  }
}